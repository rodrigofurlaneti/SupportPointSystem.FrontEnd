import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.furlaneti.fsipoint',
  appName: 'FSI Point System',
  webDir: 'dist'
};

export default config;
